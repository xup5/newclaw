// Channel self-registration barrel.
// Each import triggers the channel module's registerChannelAdapter() call.
//
// Main ships with the local CLI channel and the bundled Discord adapter.

import './cli.js';
import './discord.js';
